{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd licencas-server && npm install",
    "nixpacksPlan": {
      "providers": ["node"]
    }
  },
  "deploy": {
    "startCommand": "cd licencas-server && node index.js"
  }
}